image:
  file: .gitpod.Dockerfile

tasks:
  - name: mongodb
    command: mongod

  - name: server app
    init: cd /workspace/CodeStats/server-reactive && ./gradlew build
    command: cd /workspace/CodeStats/server-reactive &&./gradlew bootRun

vscode:
  extensions:
    - vscjava.vscode-java-pack
